{
  "name": "animeshj9-lab",
  "private": true,
  "version": "0.2.0",
  "description": "Animesh's lab: FeedProof and Long Arc",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-static.mjs"
  },
  "engines": { "node": ">=22" }
}
