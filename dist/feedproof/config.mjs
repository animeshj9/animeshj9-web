// Public configuration only. Never put API keys or credentials in this file.
// Activate only after agreeing fulfilment capacity, commercial terms and privacy handling.
export const PILOT = Object.freeze({ enabled: false, checkoutURL: '' });
